info:
  name: Create user
  type: http
  seq: 1

http:
  method: POST
  url: "{{basse_url}}/user"
  body:
    type: json
    data: |-
      {
        "username":"mio",
        "email": "mio@example.org",
        "password":"password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
